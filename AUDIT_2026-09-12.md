# Release audit — 12 September 2026

## Confirmed correction

- The 1-2-1 profile reminder UI previously assigned the fallback status `failed` when the delivery API itself returned `ok`. It now uses `sent` only when an individual result is absent; an explicit provider result still wins. This is a presentation-state correction only and did not send any LINE messages.

## Verification completed

- All Deno function tests: 162 passed, 0 failed.
- JavaScript syntax check for every file in `public/assets/js`.
- Deno type checks for non-test Edge Function files.
- Static guards for mobile information architecture, notifications, service worker safety, and team identity.
- Production negative-auth smoke checks for API, admin API, LIFF API, and cron endpoints. These did not use member writes, valid-user credentials, or message delivery.

## Deferred technical debt

- `node scripts/check-web-budgets.mjs` currently fails because `public/dashboard.html` is 3,581 bytes above its 210 KB budget and `public/liff/index.html` is 3,962 bytes above its 200 KB budget. Several related JS/CSS files are also close to their limits.
- Do not increase the budgets to mask this. The next maintenance session should split page-specific inline assets into versioned modules and re-run responsive visual acceptance with the browser test runtime available.
- The visual `test-verification-help-ui.mjs` suite could not run in this environment because the `playwright` package is not installed. It needs to be restored before treating this audit as signed-in visual acceptance.
