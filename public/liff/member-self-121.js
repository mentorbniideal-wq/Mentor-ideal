window.memberSelf121Load=async function(force=false){const box=$('#my121Self');if(!box||box.dataset.loaded==='1'&&!force)return;box.innerHTML='<div class="oto-history-empty">กำลังโหลดคำชวนและการนัดเอง…</div>';const preview=new URLSearchParams(location.search).get('preview')==='1';const r=preview?{ok:true,incoming:[{id:'self-invite-preview',note:'อยากคุยเรื่อง Referral ในกลุ่ม SME',counterpart:{nickname:'มินตรา',profession:'นักวางแผนการเงิน'}}],outgoing:[],sessions:[{id:'self-pair-preview',status:'matched',round:{meeting_date:'2026-09-11'},counterpart:{nickname:'นนท์',profession:'ที่ปรึกษาการตลาด'}}]}:await api({action:'get-member-self-121'},{latestKey:'member-self-121'});if(!r.ok){box.innerHTML=`<div class="oto-history-empty">${escGuided(r.error||'โหลดการนัดเองไม่สำเร็จ')}<br><button class="oto-history-refresh" type="button" onclick="loadMemberSelf121(true)">ลองใหม่</button></div>`;return;}box.dataset.loaded='1';setMy121Badge('#my121SelfBadge',(r.incoming||[]).filter(x=>x.status==='pending').length);const incoming=(r.incoming||[]).filter(x=>x.status==='pending'),outgoing=(r.outgoing||[]),sessions=(r.sessions||[]);box.innerHTML=`<div class="my121-self-guidance"><b>อยากเริ่มเอง?</b><br>เปิด Directory เลือกสมาชิกที่เปิดแชร์ Referral Focus แล้วกด “ชวน 1-2-1” ระบบจะสร้าง MY121 หลังอีกฝ่ายตอบรับเท่านั้น<button type="button" onclick="show('directory')">เปิด Directory</button></div><div class="my121-self-list" style="margin-top:12px">${incoming.length?`<section><b style="font-size:13px">คำชวนที่รอคุณตอบ</b>${incoming.map(x=>`<article class="my121-self-item"><b>คุณ ${memberSelf121Name(x)}</b><small>${escGuided(x.counterpart?.profession||'สมาชิก Chapter')}${x.note?`<br>“${escGuided(x.note)}”`:''}</small><div class="my121-self-item-actions"><button type="button" onclick="respondMemberSelf121Invite('${escGuided(x.id)}','declined',this)">ยังไม่สะดวก</button><button class="primary" type="button" onclick="respondMemberSelf121Invite('${escGuided(x.id)}','accepted',this)">ตอบรับ · เลือกวันนัด</button></div></article>`).join('')}</section>`:''}${sessions.length?`<section><b style="font-size:13px">MY121 ที่นัดเอง</b>${sessions.map(x=>`<article class="my121-self-item"><b>คุณ ${memberSelf121Name(x)}</b><small>${escGuided(x.counterpart?.profession||'สมาชิก Chapter')} · ${escGuided(x.status||'matched')} · เริ่ม ${escGuided(x.round?.meeting_date||'')}</small><div class="my121-self-item-actions"><button class="primary" type="button" onclick="openMemberSelf121Pair('${escGuided(x.id)}')">เปิด MY121 นี้</button></div></article>`).join('')}</section>`:''}${outgoing.length?`<section><b style="font-size:13px">คำชวนที่คุณส่ง</b>${outgoing.map(x=>`<article class="my121-self-item"><b>คุณ ${memberSelf121Name(x)}</b><small>${escGuided(x.status==='pending'?'รอการตอบรับ':x.status||'')}</small></article>`).join('')}</section>`:''}</div>`;};
