(function(){
  'use strict';
  var cache=null,loading=false;
  function e(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'})[c];});}
  function date(v){if(!v)return'ยังไม่เคยส่ง';try{return new Date(v).toLocaleString('th-TH',{dateStyle:'short',timeStyle:'short'});}catch(_){return String(v);}}
  function kpi(label,value,sub,tone){return'<div class="lac-kpi"><small>'+e(label)+'</small><b class="'+(tone||'')+'">'+e(value)+'</b><span>'+e(sub||'')+'</span></div>';}
  function quotaText(q){if(!q||!q.ok)return'ตรวจไม่ได้';return q.unlimited?'∞':Number(q.remaining||0).toLocaleString('th-TH');}
  function card(x,canEdit){var s=x.stats||{},enabled=!!x.enabled,protectedFlag=!!x.protected,decision={keep:'ควรเก็บ',limit:'ใช้แบบพอดี',disable:'ซ้ำ / ควรปิด',system:'งานระบบ',review:'รอตรวจ'}[x.decision]||'รอตรวจ';return'<article class="lac-panel lac-card '+(enabled?'':'off')+'"><div class="lac-card-head"><div><div class="lac-title">'+e(x.name)+'</div><div class="lac-tags"><span class="lac-tag decision-'+e(x.decision)+'">'+e(decision)+' · '+e(x.value_score||0)+'/100</span><span class="lac-tag '+e(x.importance)+'">'+e(x.importance)+'</span><span class="lac-tag">Quota '+e(x.quota_impact)+'</span><span class="lac-tag">'+e(x.module)+'</span></div></div><button class="lac-toggle '+(enabled?'on':'')+'" '+((!canEdit||protectedFlag)?'disabled':'')+' onclick="lineAutoToggle(\''+e(x.automation_key)+'\','+(!enabled)+')">'+(protectedFlag?'🔒 จำเป็น':enabled?'เปิดอยู่':'ปิดอยู่')+'</button></div><dl class="lac-detail"><dt>เวลา</dt><dd>'+e(x.schedule_label)+'</dd><dt>ผู้รับ</dt><dd>'+e(x.audience)+'</dd><dt>เหตุผล</dt><dd>'+e(x.purpose)+'</dd><dt>30 วัน</dt><dd>ส่ง '+e(s.sent||0)+' · ล้มเหลว '+e(s.failed||0)+' · ระงับ '+e(s.suppressed||0)+'</dd><dt>ล่าสุด</dt><dd>'+e(date(s.lastSentAt))+'</dd><dt>ต้นทาง</dt><dd>'+e(x.source)+' / '+e(x.notification_type||'ไม่ส่งข้อความ')+'</dd></dl><div class="lac-rec">'+(x.duplicate_note?'🔎 '+e(x.duplicate_note)+'<br>':'')+'💡 '+e(x.recommendation||'ทบทวนตามประโยชน์และจำนวนผู้รับจริง')+'</div><div class="lac-card-foot"><span>แก้ล่าสุด '+e(date(x.updated_at))+'</span><span>'+(x.updated_by?'โดย '+e(x.updated_by):'ค่าเริ่มต้นระบบ')+'</span></div></article>';}
  function render(r){var q=r.quota||{},s=r.summary||{},controls=r.controls||[],broad=controls.filter(function(x){return x.enabled&&x.quota_impact==='high';}).length;var badge=document.getElementById('badge-line-auto');if(badge){badge.textContent=broad||'';badge.style.display=broad?'inline-flex':'none';}var html='<div class="lac-kpis">'+kpi('LINE คงเหลือ',quotaText(q),q.unlimited?'Unlimited':('ส่งแล้ว '+Number(q.used||0).toLocaleString('th-TH')),(q.remaining!=null&&q.remaining<100)?'lac-bad':'lac-good')+kpi('Auto ทั้งหมด',s.total||0,'ทุกระบบใน Desktop','')+kpi('เปิดใช้งาน',s.enabled||0,'รวมงานระบบที่จำเป็น','lac-good')+kpi('Broadcast กว้าง',s.broadEnabled||0,'ควรเหลือให้น้อยที่สุด',s.broadEnabled>1?'lac-warn':'lac-good')+kpi('ส่งเดือนนี้',s.monthSent||0,'จาก Delivery Log','')+'</div>';
    html+='<section class="lac-panel lac-toolbar"><div><h3>รูปแบบการส่ง</h3><p>แนะนำ “เฉพาะจำเป็น” เพื่อให้สมาชิกได้รับเฉพาะข้อความที่นำไปทำต่อได้</p></div><div class="lac-presets">'+(r.canEdit?'<button onclick="lineAutoPreset(\'essential\')">✓ เฉพาะจำเป็น</button><button onclick="lineAutoPreset(\'balanced\')">สมดุล</button><button onclick="lineAutoPreset(\'all\')">เปิดทั้งหมด</button>':'<span class="lac-tag">Mentor Co. ดูได้ · Chapter Admin แก้ไขได้</span>')+'</div></section>';
    var groups=[['keep','ควรเก็บ — ส่งเฉพาะเมื่อมีเรื่องให้ทำ'],['limit','ใช้แบบพอดี — จำกัดความถี่'],['disable','ซ้ำหรือประโยชน์ต่ำ — ปิดไว้'],['review','ค้นพบใหม่ — รอตรวจ'],['system','งานระบบ — ไม่กินโควตา']];groups.forEach(function(g){var rows=controls.filter(function(x){return (x.decision||'review')===g[0];}).sort(function(a,b){return Number(b.value_score||0)-Number(a.value_score||0);});if(rows.length)html+='<div class="lac-group"><h3>'+e(g[1])+' <span>'+rows.length+' รายการ</span></h3><div class="lac-grid">'+rows.map(function(x){return card(x,r.canEdit);}).join('')+'</div></div>';});
    var recent=r.recent||[];html+='<section class="lac-panel lac-recent"><h3>ข้อความ Auto ล่าสุด</h3>'+(recent.length?recent.map(function(x){return'<div class="lac-recent-row"><b>'+e(x.notification_type||'system')+'</b><span>'+e(x.status)+'</span><span>'+e(x.estimated_count||1)+' msg</span><span>'+e(date(x.sent_at||x.created_at))+'</span></div>';}).join(''):'<div class="lac-state">ยังไม่มี Delivery Log จาก cron-jobs</div>')+'</section>';
    document.getElementById('line-auto-root').innerHTML=html;
  }
  window.loadLineAutoControlCenter=function(force){if(loading)return;if(cache&&!force){render(cache);return;}loading=true;var root=document.getElementById('line-auto-root');if(root)root.innerHTML='<div class="lac-state">กำลังตรวจทุกระบบที่ส่ง LINE อัตโนมัติ…</div>';gsr('getLineAutoControlCenter',{days:30},function(r){loading=false;if(!r||!r.ok){root.innerHTML='<div class="lac-state lac-bad">โหลด LINE AUTO ไม่สำเร็จ<br>'+e(r&&r.error||'unknown')+'</div>';return;}cache=r;render(r);});};
  window.lineAutoToggle=function(key,enabled){if(!confirm((enabled?'เปิด':'ปิด')+'ข้อความอัตโนมัตินี้?'))return;gsr('setLineAutomationControl',{automationKey:key,enabled:enabled},function(r){if(!r||!r.ok){toast('❌ '+(r&&r.error||'บันทึกไม่ได้'),'err');return;}toast('✅ บันทึกแล้ว','ok');cache=null;loadLineAutoControlCenter(true);});};
  window.lineAutoPreset=function(preset){var labels={essential:'เฉพาะจำเป็น',balanced:'สมดุล',all:'เปิดทั้งหมด'};if(!confirm('ใช้รูปแบบ “'+labels[preset]+'” กับข้อความ Auto ทุกระบบ?'))return;gsr('applyLineAutomationPreset',{preset:preset},function(r){if(!r||!r.ok){toast('❌ '+(r&&r.error||'ปรับรูปแบบไม่ได้'),'err');return;}toast('✅ ใช้รูปแบบ '+labels[preset]+' แล้ว','ok');cache=null;loadLineAutoControlCenter(true);});};
})();
